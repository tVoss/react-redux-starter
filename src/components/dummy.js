import React from 'react';

// An example of a dumb component that doesn't rely on global state
export default class Dummy extends React.Component {   
    render() {
        return (
            <div>Hello World!</div>
        )
    }
}